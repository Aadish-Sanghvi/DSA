#include<iostream>
#include<stack>
using namespace std;

int findMinimumCost(string str){
    //odd condition
    if(str.length % 2 == 1)
    return -1;

    stack<char> s;

    for(int i = 0; i < str.length(); i++){
        char ch = str[i];
        
    }
}